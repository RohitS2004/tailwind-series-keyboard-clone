import type { IKey } from "../types";

const Key = (prop: IKey) => {
  return <div></div>;
};

export default Key;

// Half (arrow_group_vertical, function, delete, escape)
// Regular (alphabetical, arrow, control, group, regular)
// Tab (tab)
// Capslock (capslock)
// Shift left (shift)
// Spacebar (spacebar)
// Arrow Up/Down
// Backspace (backspace)
// Shift right (shift)
// Enter (enter)
// Backspace (backspace)
